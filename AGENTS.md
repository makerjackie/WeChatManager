# 开发约定

- 所有用户界面与用户可见提示使用简体中文。
- 采用 Swift 6 严格并发与原生 SwiftUI/AppKit API。
- 不得静默删除微信聊天数据；缓存清理必须移动到废纸篓并允许恢复。
- 修改微信前必须检测精确构建版本并创建完整备份；不兼容版本必须拒绝执行。
- 发布产物必须使用 Developer ID 签名、Apple 公证并附加公证票据。
- 自动更新使用 Sparkle 2、HTTPS appcast 与 EdDSA 签名。
