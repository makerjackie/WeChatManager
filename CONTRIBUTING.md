# 贡献指南

欢迎提交兼容性修复、文件路径适配、无障碍改进和测试。

提交前请确保：

1. 所有用户可见文本为简体中文。
2. 不记录或上传任何微信聊天数据。
3. 新增清理路径必须有严格白名单测试。
4. 新增补丁必须绑定精确微信构建号并提供来源依据。
5. 运行 `xcodegen generate` 和完整 `xcodebuild test`。
6. 不提交证书、API Key、Sparkle 私钥或其他发布凭据。
